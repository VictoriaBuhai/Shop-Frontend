import { makeStyles } from "@mui/styles";

export const BasketDialogStyles = makeStyles(() => ({
  orders: {
    display: "flex",
    width: "400px",
    flexDirection: "column",
    gap: "15px",
    backgroundColor: "azure",
    alignItems: "center",
    padding: "20px",
  },
}));
