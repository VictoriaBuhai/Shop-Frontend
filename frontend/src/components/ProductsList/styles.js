import { makeStyles } from "@mui/styles";

export const ProductListStyles = makeStyles(() => ({
  list: {
    maxWidth: "1400px",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    flexWrap: "wrap",
    gap: "30px",
    marginBottom: "50px",
  },
}));
